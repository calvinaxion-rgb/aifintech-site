# worker/wrangler.toml
name = "aifintech-api"
main = "src/index.js"
compatibility_date = "2024-12-07"

# KV 命名空間（用於儲存用戶與用量）
[[kv_namespaces]]
binding = "KV"
id = "your-kv-namespace-id"

# 環境變數（實際 Key 用 wrangler secret 設定）
[vars]
STRIPE_WEBHOOK_SECRET = ""
GROQ_API_KEY = ""